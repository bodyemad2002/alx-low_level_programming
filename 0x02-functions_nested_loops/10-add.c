#include "main.h"
#include <stdio.h>
/**
  * add - prints every minute of the day of Jack Bauer
  * @x: The character to be checked
  * @y: The character to be checked
  * Return: Always 0
  */
int add(int x, int y)
{
	int z;

	z = x + y;
	_putchar(z);
	return (z);
}
