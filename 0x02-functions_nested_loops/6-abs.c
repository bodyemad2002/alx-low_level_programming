#include "main.h"
#include <stdio.h>
/**
  * _abs - computes the absolute value
  * @int: The character to be checked
  * Return: Always 0
  */
int _abs(int)
{
	if (int < 0)
		return (-int);
	else
		return (int);
}
