#include "main.h"
#include <stdio.h>
/**
  * _abs - computes the absolute value
  * @r: The character to be checked
  * Return: Always 0
  */
int _abs(int)
{
	if (r < 0)
		return (-r);
	else
		return (r);
}
