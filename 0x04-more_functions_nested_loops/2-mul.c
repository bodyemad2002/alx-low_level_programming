#include "main.h"
#include <stdio.h>

/**
  * mul - Checks for uppercase character
  * @a: The character to be checked
  * @b: The character to be checked
  *
  * Return: Always 0
  */
int mul(int a, int b)
{
	printf("%d\n", mul(a, b));
	return (0);
}
