#include "main.h"
#include <stdio.h>
/**
  * jack_bauer - prints every minute of the day of Jack Bauer
  * @void: The character to be checked
  * Return: Always 0
  */
void jack_bauer(void)
{
	int x;

	for (x = 00 : 00; x < 24 : 00; x++)
		printf(x);
	return (x);
}
